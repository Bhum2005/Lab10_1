//TIP To <b>Run</b> code, press <shortcut actionId="Run"/> or
// click the <icon src="AllIcons.Actions.Execute"/> icon in the gutter.
void main() {
    Shape s1 = ShapeFactory.getShape(0);
    s1.draw();
    Shape s2 = ShapeFactory.getShape(0);
    s2.draw();
    Shape s3 = ShapeFactory.getShape(0);
    Shape s4 = ShapeFactory.getShape(1);
    s4.draw();
    Shape s5 = ShapeFactory.getShape(1);
    s4.draw();
    Shape s6 = ShapeFactory.getShape(1);
    Shape s7 = ShapeFactory.getShape(2);
    s7.draw();
    Shape s8 = ShapeFactory.getShape(2);
    Shape s9 = ShapeFactory.getShape(2);
}
